const connection = require('../config/database')

const getAllByBarbearia = (id,callback)=>{}
const getById = (id,callback)=>{}
const save = (callback)=>{}
const update = (id,callback)=>{}
const delete_ = (id,callback)=>{}

module.exports = {
    getAllByBarbearia,
    getById,
    save,
    update,
    delete_
}